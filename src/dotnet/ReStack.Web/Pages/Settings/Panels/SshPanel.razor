@inherits Panel

@if (Visible)
{
    <div class="flex flex-wrap md:flex-nowrap">
        <div class="w-full mb-2">
            <label class="w-full mb-4">
                SSH Key
            </label>
            <p class="italic text-xs pe-2">This key is generated once at startup & used to connect to remote hosts</p>
        </div>

        <div class="w-full">
            <input type="password" value="@Guid.NewGuid()" class="w-full block mt-2 w-full bg-gray-200 dark:bg-neutral-600 text-gray-700 dark:text-gray-100 text-sm border-none rounded py-2 md:py-3 focus:bg-white focus:border-gray-500 focus:ring-0" />

            <div class="flex mt-4 justify-end">
                <div class="pe-1">
                    <ButtonCopy CssClass="btn-secondary" Value="GetSshKey">
                        Copy
                    </ButtonCopy>
                </div>
                <div class="ps-1">
                    <button class="btn-secondary"
                            @onclick="Regenerate">
                        Regenerate
                    </button>
                </div>
            </div>
        </div>
    </div>
}